path
# exec_one(s, s->path)
# exec_many(s, s->path)
# exec_one_fork(s, s->path)
# exec_cmd_loop(s, s->path, pipe1, pipe2)
# in_redir(s, i, s->path)
# out_redir(s, i, s->path)
# exit_forks("pipe setter failed to set stdou/stdin", 1, s, s->path)
# exec_ve_err(t_script *s, char **path, int i)
# void	exec_go(t_script *s, char **path, int id, int i)
heredoc(t_script *s, int i, char **path)